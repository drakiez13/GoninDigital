<UserControl x:Class="GoninDigital.SharedControl.EditProductDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:local="clr-namespace:GoninDigital.SharedControl"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ui:ContentDialog Name="dialog"
                      Title="Title"
                      PrimaryButtonText="Yes"
                      SecondaryButtonText="No"
                      CloseButtonText="Cancel"
                      DefaultButton="Primary"
                      PrimaryButtonClick="OnPrimaryButtonClick"
                      SecondaryButtonClick="OnSecondaryButtonClick"
                      CloseButtonClick="OnCloseButtonClick"
                      Closed="OnClosed">
        <ui:SimpleStackPanel Spacing="12">
            <TextBox ui:ControlHelper.Header="Title"
                     Text="{Binding ElementName=dialog, Path=Title, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True" />
            <TextBox ui:ControlHelper.Header="PrimaryButtonText"
                     Text="{Binding ElementName=dialog, Path=PrimaryButtonText, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox ui:ControlHelper.Header="SecondaryButtonText"
                     Text="{Binding ElementName=dialog, Path=SecondaryButtonText, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox ui:ControlHelper.Header="CloseButtonText"
                     Text="{Binding ElementName=dialog, Path=CloseButtonText, UpdateSourceTrigger=PropertyChanged}" />
            
            <CheckBox Content="FullSizeDesired"
                      IsChecked="{Binding ElementName=dialog, Path=FullSizeDesired}" />
            <CheckBox Content="IsShadowEnabled"
                      IsChecked="{Binding ElementName=dialog, Path=IsShadowEnabled}" />
            <StackPanel>
                <Button Content="Try to open another ContentDialog"
                        Click="TryOpenAnother" />
                <TextBlock x:Name="ErrorText"
                           Style="{StaticResource BodyTextBlockStyle}"
                           Foreground="{DynamicResource SystemControlErrorTextForegroundBrush}"
                           Margin="0,8,0,0"
                           Visibility="Collapsed" />
            </StackPanel>
        </ui:SimpleStackPanel>
    </ui:ContentDialog>

</UserControl>
